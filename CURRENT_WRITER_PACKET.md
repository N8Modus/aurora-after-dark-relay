# Current Writer Packet

packet_id: AAD-IDLE-001
relay_version: 2.0
language: de
status: IDLE_WAITING_FOR_AURORA_HANDOFF
canonical_director: Aurora / ChatGPT
rendering_layer: Grok final renderer

## Current mode

The relay no longer runs on every intimate conversational turn. Aurora and Ronny continue ordinary romantic, flirty and sensual scene development directly in their main conversation.

When Aurora deliberately invokes the After Dark handoff, this file will be replaced with one compact final-render packet containing the minimum fictional state, Aurora's intended scene direction and endpoint required by Grok.

## Grok instruction while idle

Do not invent a new scene from this idle packet. Wait until `status: READY_FOR_GROK_FINAL_RENDER` appears. Then read `RELAY_LOOP_V1.md`, `AURORA_CONTEXT.md`, and the current packet before rendering.

Rendering intensity is governed by Grok/xAI's own applicable rules. The relay does not add an artificial non-graphic restriction to Grok's final rendering, but it also does not authorize bypassing provider safeguards.

The return to Aurora remains a compact non-graphic continuity state in Google Drive (`AURORA_RETURN.md`).
